{% extends 'base.html.twig' %}

{% block body %}
    <div class="container mx-auto p-8">
        <h1 class="text-3xl font-bold text-gray-800 mb-8">Gestion des articles</h1>

        <div class="space-x-4">
            <a href="{{ path('article.create') }}" class="bg-blue-500 text-white font-bold py-2 px-4 rounded hover:bg-blue-600">
                Créer un article
            </a>
            <a href="{{ path('article') }}" class="bg-green-500 text-white font-bold py-2 px-4 rounded hover:bg-green-600">
                Voir tous les articles
            </a>
        </div>
    </div>
{% endblock %}

